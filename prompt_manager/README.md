# Prompt Manager

Веб-интерфейс для управления стадиями диалога (агентами) в системе LOOKTOWN Bot.

## Возможности

- 📝 **Редактирование промптов** - изменение промптов стадий через удобный интерфейс
- ➕ **Создание новых стадий** - создание новых агентов с настройкой инструментов
- 🗑️ **Удаление стадий** - полное удаление стадий из системы (файл, YDB, Yandex Cloud)
- 🔄 **Автоматическая синхронизация** - изменения применяются к файлам и Assistant в Yandex Cloud

## Запуск

```powershell
python prompt_manager/run.py
```

Или напрямую через Streamlit:

```powershell
streamlit run prompt_manager/app.py --server.port 8502
```

Браузер откроется автоматически по адресу: http://localhost:8502

## Использование

### Редактирование промпта стадии

1. Выберите стадию в боковой панели
2. Отредактируйте промпт в текстовом поле
3. Нажмите "💾 Сохранить"
4. Промпт будет обновлён в файле и в Assistant в Yandex Cloud

### Создание новой стадии

1. Нажмите "➕ Создать новую стадию" в боковой панели
2. Заполните форму:
   - **Название стадии** - отображаемое имя (например: "Агент консультации")
   - **Ключ стадии** - используется для имени файла (например: "consultation")
   - **Промпт стадии** - инструкция для агента
   - **Инструменты** - выберите нужные инструменты
3. Нажмите "✅ Создать"
4. ⚠️ **Важно**: После создания нужно вручную добавить стадию в `booking_graph.py`

### Удаление стадии

1. Выберите стадию в боковой панели
2. Нажмите "🗑️ Удалить стадию"
3. Подтвердите удаление
4. Стадия будет удалена из:
   - Файла агента
   - YDB (база данных)
   - Yandex Cloud (Assistant)
5. ⚠️ **Важно**: После удаления нужно вручную удалить стадию из `booking_graph.py`

## Структура

```
prompt_manager/
├── app.py              # Основной веб-интерфейс (Streamlit)
├── stage_manager.py    # Логика работы со стадиями
├── run.py              # Скрипт запуска
└── __init__.py
```

## Технические детали

- **Порт**: 8502 (чтобы не конфликтовать с playground на 8501)
- **Формат промптов**: Тройные кавычки в Python файлах
- **Синхронизация**: Автоматическое обновление Assistant в Yandex Cloud при сохранении
- **Удаление**: Полная очистка всех упоминаний стадии в системе

## Примечания

⚠️ После создания или удаления стадии необходимо вручную обновить `src/graph/booking_graph.py`:
- Добавить импорт нового агента
- Добавить агента в кэш `_agents_cache`
- Добавить обработчик в граф
- Добавить маршрутизацию в `_route_by_stage`


