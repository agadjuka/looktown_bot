# 🔐 АНАЛИЗ ИСПОЛЬЗОВАНИЯ КЛЮЧА СЕРВИСНОГО АККАУНТА YANDEX CLOUD

**Дата анализа:** 2025-01-20  
**Проект:** beauty_salon_ai  
**Целевая платформа:** Yandex Cloud Serverless Container

---

## 📋 РЕЗЮМЕ

**ВЫВОД:** Ключ сервисного аккаунта (`YC_SA_KEY`) **НЕ должен** передаваться в переменные окружения контейнера. Он используется только на этапе деплоя для аутентификации в GitHub Actions. Внутри контейнера аутентификация происходит автоматически через привязанный сервисный аккаунт.

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ

### 1. Использование `YC_SA_KEY` в GitHub Actions

**Расположение:** `.github/workflows/deploy-to-ycr.yml`

#### Где используется `YC_SA_KEY`:

1. **Вход в Container Registry** (строка 20):
   ```yaml
   - name: Login to Yandex Container Registry
     uses: yc-actions/yc-cr-login@v1
     with:
       yc-sa-json-credentials: ${{ secrets.YC_SA_KEY }}
   ```
   **Назначение:** Аутентификация в GitHub Actions для доступа к Container Registry для push образа.

2. **Деплой контейнера** (строка 42):
   ```yaml
   - name: Deploy Serverless Container
     uses: yc-actions/yc-sls-container-deploy@v1
     with:
       yc-sa-json-credentials: ${{ secrets.YC_SA_KEY }}
       # ...
   ```
   **Назначение:** Аутентификация в GitHub Actions для создания новой revision контейнера.

#### Где НЕ используется:

❌ **В переменных окружения контейнера** (`revision-env`, строки 49-61):
   - `YC_SA_KEY` отсутствует в списке переменных окружения
   - В список включены только: `DATABASE_URL`, `TELEGRAM_BOT_TOKEN`, `LLM_PROVIDER`, `LOG_MODE`, `S3_*`, `GOOGLE_APPLICATION_CREDENTIALS`, `YANDEX_*`, `YDB_ENDPOINT`, `YDB_DATABASE`

---

### 2. Настройка сервисного аккаунта в контейнере

**Расположение:** `.github/workflows/deploy-to-ycr.yml`, строка 46

```yaml
revision-service-account-id: 'aje7vj62jhsh2qjmuefm'
```

**Что это означает:**
- Контейнеру **привязан** сервисный аккаунт с ID `aje7vj62jhsh2qjmuefm`
- Этот сервисный аккаунт используется для автоматической аутентификации внутри контейнера
- SDK YDB автоматически получает IAM токен через метаданные инфраструктуры Яндекс.Облака

---

### 3. Аутентификация в коде приложения

**Расположение:** `app/core/database.py`, функция `get_driver()` (строки 23-99)

#### Три способа аутентификации (в порядке приоритета):

1. **Автоматическая аутентификация** (строки 35-45):
   ```python
   driver_config = ydb.DriverConfig(
       endpoint=endpoint,
       database=database,
       # Не указываем credentials - SDK сам получит IAM токен
   )
   ```
   - ✅ **Работает в Serverless Container** с привязанным сервисным аккаунтом
   - SDK автоматически получает IAM токен через метаданные
   - **Не требует** файла `key.json` или переменных окружения

2. **Аутентификация через метаданные** (строки 52-61):
   ```python
   driver_config = ydb.DriverConfig(
       endpoint=endpoint,
       database=database,
       credentials=ydb.iam.MetadataUrlCredentials(),
   )
   ```
   - Работает на VM с привязанным сервисным аккаунтом
   - Fallback, если автоматическая аутентификация не сработала

3. **Аутентификация через локальный файл** (строки 67-92):
   ```python
   service_account_key_file = os.getenv("YC_SERVICE_ACCOUNT_KEY_FILE", "key.json")
   # ...
   credentials=ydb.iam.ServiceAccountCredentials.from_file(key_file_path),
   ```
   - ⚠️ **Только для локальной разработки**
   - Требует файл `key.json` в корне проекта
   - Используется, если автоматическая аутентификация и метаданные не работают

---

### 4. Файл `key.json` в проекте

**Статус:** ✅ Находится в корне проекта, но:

- ❌ **НЕ копируется** в Docker образ (отсутствует в `Dockerfile`)
- ❌ **Исключен** из образа через `.dockerignore` (файл `.env` исключен, но `key.json` не упомянут явно)
- ✅ **Используется только** для локальной разработки
- ⚠️ **НЕ должен** попадать в git (добавлен в `.gitignore`)

---

### 5. Dockerfile

**Расположение:** `Dockerfile`

**Что копируется в образ:**
```dockerfile
COPY app ./app
COPY dialogue_patterns.json .
COPY scripts ./scripts
```

**Что НЕ копируется:**
- ❌ `key.json` - отсутствует в списке COPY
- ❌ `.env` - исключен через `.dockerignore`

---

## ✅ ВЫВОДЫ И РЕКОМЕНДАЦИИ

### Текущая архитектура (ПРАВИЛЬНАЯ):

1. ✅ **GitHub Actions использует `YC_SA_KEY`** только для:
   - Входа в Container Registry
   - Деплоя контейнера

2. ✅ **Контейнер получает сервисный аккаунт** через:
   - `revision-service-account-id: 'aje7vj62jhsh2qjmuefm'`
   - Автоматическая аутентификация через метаданные инфраструктуры

3. ✅ **Код приложения** использует автоматическую аутентификацию:
   - Не требует файла `key.json` внутри контейнера
   - Не требует переменных окружения с ключом

### ❌ НЕ нужно делать:

1. ❌ **НЕ передавать** `YC_SA_KEY` в `revision-env` в workflow
2. ❌ **НЕ копировать** `key.json` в Docker образ
3. ❌ **НЕ создавать** переменную окружения `YC_SA_JSON_CREDENTIALS` в контейнере
4. ❌ **НЕ создавать** переменную окружения `YC_SERVICE_ACCOUNT_KEY_FILE` в контейнере

### ✅ Правильная архитектура для облака:

```
GitHub Actions (YC_SA_KEY) → Container Registry → Docker Image
                                      ↓
                    Serverless Container (service-account-id)
                                      ↓
                    Автоматическая аутентификация через метаданные
                                      ↓
                              YDB Connection
```

### ✅ Правильная архитектура для локальной разработки:

```
Локальная машина → key.json файл → YDB Connection
```

---

## 🔧 ПРОВЕРКА ТЕКУЩЕЙ КОНФИГУРАЦИИ

### ✅ Все правильно настроено:

1. ✅ В `.github/workflows/deploy-to-ycr.yml`:
   - `YC_SA_KEY` используется только для `yc-cr-login` и `yc-sls-container-deploy`
   - `YC_SA_KEY` **НЕ** передается в `revision-env`
   - Указан `revision-service-account-id`

2. ✅ В `Dockerfile`:
   - `key.json` **НЕ** копируется в образ

3. ✅ В `app/core/database.py`:
   - Реализована автоматическая аутентификация
   - Есть fallback на локальный файл только для разработки

---

## 📝 СПРАВОЧНАЯ ИНФОРМАЦИЯ

### Переменные окружения в контейнере:

**Текущий список** (из `.github/workflows/deploy-to-ycr.yml`):
- `DATABASE_URL`
- `TELEGRAM_BOT_TOKEN`
- `LLM_PROVIDER`
- `LOG_MODE`
- `S3_ACCESS_KEY_ID`
- `S3_SECRET_ACCESS_KEY`
- `S3_BUCKET_NAME`
- `GOOGLE_APPLICATION_CREDENTIALS`
- `YANDEX_FOLDER_ID`
- `YANDEX_API_KEY_SECRET`
- `YDB_ENDPOINT`
- `YDB_DATABASE`

### Переменные, связанные с сервисным аккаунтом:

- ❌ `YC_SA_KEY` - используется только в GitHub Actions, **НЕ** в контейнере
- ❌ `YC_SA_JSON_CREDENTIALS` - **НЕ используется** в проекте
- ❌ `YC_SERVICE_ACCOUNT_KEY_FILE` - используется только для локальной разработки, **НЕ** нужен в контейнере

---

## 🎯 ИТОГОВЫЙ ОТВЕТ

**Вопрос:** Нужно ли передавать ключ сервисного аккаунта в переменные окружения контейнера?

**Ответ:** ❌ **НЕТ, не нужно и не должно.**

**Причина:**
1. В Serverless Container аутентификация происходит автоматически через привязанный сервисный аккаунт (`revision-service-account-id`)
2. SDK YDB автоматически получает IAM токен через метаданные инфраструктуры
3. Передача ключа в переменные окружения:
   - Излишня (не используется)
   - Небезопасна (ключ будет виден в переменных окружения)
   - Противоречит best practices Яндекс.Облака

**Текущая конфигурация правильная и соответствует best practices.**

---

**Конец отчета**

