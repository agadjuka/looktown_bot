# LangGraph Agent Playground

## Описание

Streamlit Playground для интерактивного тестирования LangGraph агентов. Позволяет:
- 💬 Переписываться с агентом в реальном времени
- 🔧 Видеть какие инструменты вызываются
- 📊 Отслеживать состояния графа
- 📜 Просматривать историю Thread
- 🔍 Видеть метаданные (стадии, извлечённая информация)

## Запуск

### Вариант 1: Через скрипт
```bash
python run_playground.py
```

### Вариант 2: Напрямую через Streamlit
```bash
streamlit run playground.py
```

## Требования

- Установлены все зависимости из `requirements.txt`
- Настроены переменные окружения в `.env`:
  - `YANDEX_FOLDER_ID`
  - `YANDEX_API_KEY_SECRET`

## Использование

1. Запустите playground
2. Введите сообщение в поле ввода
3. Наблюдайте за работой агента:
   - В боковой панели видны вызовы инструментов
   - В боковой панели видны состояния графа
   - В основном окне - диалог с агентом
   - В деталях каждого ответа - метаданные

## Функции

- **Сброс диалога** - создаёт новый Thread и очищает историю
- **История инструментов** - показывает последние 10 вызовов инструментов
- **Состояния графа** - показывает последние 5 состояний графа
- **История Thread** - показывает полную историю переписки в Thread

